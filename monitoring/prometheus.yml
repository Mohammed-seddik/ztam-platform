global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: auth-middleware
    metrics_path: /metrics
    static_configs:
      - targets:
          - auth-middleware:8001
